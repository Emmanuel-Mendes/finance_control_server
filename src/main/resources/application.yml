server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: kjadnsfljadsgvsakcnaçslkcnlskjbvlaksddnjfcflçaksndc
  expiration: 86400000 # 24 horas em milissegundos

logging:
  level:
    br.com.financeControl: DEBUG
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG
    root: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always